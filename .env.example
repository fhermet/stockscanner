# Data Provider
# Par defaut, l'app utilise les donnees mockees (local dataset).
# Si YAHOO_ENABLED=true, elle interroge Yahoo Finance (gratuit, sans cle API).
# YAHOO_ENABLED=true

# Cache TTL (optionnel, en millisecondes)
# CACHE_STOCKS_TTL=3600000
# CACHE_STOCK_TTL=900000
